import React, {Component} from 'react'
import {connect} from 'react-redux'

class Phones extends Component {
    componentDidMount () {
        this.probs.fetchPhones()
    }
    render () {
       return (
        <div>
            Phones
        </div>
       )
    }
}

const mapDispatchToProbs = {
    // fetchPhones
}

export default connect(null, mapDispatchToProbs) (Phones)